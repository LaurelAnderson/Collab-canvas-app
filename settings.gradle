pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Collab-Canvas"
include ':app'
include ':networking'
project(':networking').projectDir = new File(rootDir, 'app/networking/')
include ':networking'
